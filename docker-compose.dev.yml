version: "3.8"

services:
  frontend:
    image: personal-website-frontend-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - /app/node_modules
      - .:/app
    ports:
      - 3001:3000
    environment:
      - WATCHPACK_POLLING=true
